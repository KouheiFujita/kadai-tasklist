<h1>タスク一覧</h1>
  <ul>
    <% @tasks.each do |task| %>
      <% if task.user_id == current_user.id %>
        <li><%= link_to task.id, task %>: <%= task.status %> : <%= task.content %></li>
      <% end %>
    <% end %>
  </ul>
<%= link_to '新規タスクの送信', new_task_path %>