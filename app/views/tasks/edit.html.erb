<h1>id = <%= @task.id %>のタスク編集ページ</h1>

<%= form_for(@task) do |f| %>
  <%= f.label :status, 'ステータス' %>
  <%= f.select :status, ["1", "2"] %>
  
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>
  
  <%= f.submit '送信' %>
<% end %>

<%= link_to '一覧へ戻る', tasks_path %>